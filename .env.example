# URL del backend. Requerida en producción.
# En desarrollo local apunta a localhost automáticamente si no se define.
VITE_API_BASE_URL=http://localhost:3001

# Producción → configurar en Vercel Dashboard > Settings > Environment Variables
# VITE_API_BASE_URL=https://<tu-space>.hf.space
